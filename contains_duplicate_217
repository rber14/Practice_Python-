# nums = [1, 2, 3, 1]

#time is o(n)
# space is o(n) - extra memory but we insert in o(1) then ask if it already exist

# dict1 = {}
# for i in range(len(nums)):
#     if nums[i] in dict1:
#         print(nums[i])
#     else:
#         dict1[nums[i]] = i

# print(dict1)

class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        set_created = set()
        for i in nums:
            if i in set_created:
                return True
            else:
                set_created.add(i)
        return False

